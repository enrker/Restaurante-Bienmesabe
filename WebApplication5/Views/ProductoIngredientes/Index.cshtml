@model IEnumerable<WebApplication5.Models.ProductoIngrediente>

@{
    ViewData["Title"] = "Index";
}

<h1>Producto Ingredientes</h1>

<p>
    <a asp-action="Create">Crear nuevo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdIngredienteNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdProductoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdIngredienteNavigation.IdIngrediente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdProductoNavigation.IdProducto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdProductoIngrediente">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdProductoIngrediente">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdProductoIngrediente">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
